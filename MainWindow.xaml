<Window x:Class="TodoApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TodoApp"
        mc:Ignorable="d"
        Title="TodoApp" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid>
        <DataGrid x:Name="dgTodoList" FontSize="16" FontWeight="Bold" Margin="10" SelectionChanged="dgTodoList_SelectionChanged"c AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding = "{Binding Path = CreationDate}" IsReadOnly="False" Header="CreationDate" Width="180"/>
                <DataGridTextColumn Binding="{Binding Path = _isDone}" IsReadOnly="True" Header="Done" Width="80"/>
                <DataGridTextColumn Binding="{Binding Path = _text}" IsReadOnly="True" Header="ToDo" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
